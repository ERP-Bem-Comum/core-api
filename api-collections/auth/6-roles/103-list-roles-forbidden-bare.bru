meta {
  name: US3 list roles bare (sem role:read)
  type: http
  seq: 103
}

get {
  url: {{baseUrl}}/api/v1/roles
  auth: none
}

headers {
  Authorization: Bearer {{bareToken}}
}

tests {
  test("SEC — 403 sem permission role:read", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
