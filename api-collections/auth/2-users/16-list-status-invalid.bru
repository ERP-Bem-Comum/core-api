meta {
  name: US1 list status invalido
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/api/v1/users?status=xpto
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("CA — status fora do enum -> 400 (Zod)", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
