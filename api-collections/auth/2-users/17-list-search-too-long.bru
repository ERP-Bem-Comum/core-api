meta {
  name: SEC list search excessivo
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/api/v1/users?search=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("SEC — search > 128 chars rejeitado na borda -> 400 (anti-DoS no LIKE)", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
