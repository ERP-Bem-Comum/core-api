meta {
  name: US1 list pageSize invalido
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/api/v1/users?pageSize=7
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("CA — pageSize fora de {5,10,25} -> 400 (validacao Zod na borda)", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
