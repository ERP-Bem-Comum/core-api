meta {
  name: US6 editar papel sem role:update (403)
  type: http
  seq: 211
}

put {
  url: {{baseUrl}}/api/v1/roles/{{newRoleId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{bareToken}}
}

body:json {
  {
    "name": "Forbidden Update"
  }
}

tests {
  test("US6 — 403 bare sem role:update", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
