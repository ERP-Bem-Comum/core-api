# Copie para .env (nao versionado) e preencha antes de rodar a colecao.
# Usado pelo bru CLI via {{process.env.E2E_SEED_PASSWORD}}.
# O script scripts/e2e-bruno-auth.sh exporta essa variavel automaticamente.
E2E_SEED_PASSWORD=<senha-do-seed-local>
