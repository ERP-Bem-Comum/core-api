meta {
  name: US7 POST /me/password-reset (solicita reset)
  type: http
  seq: 82
}

post {
  url: {{baseUrl}}/api/v1/me/password-reset
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("CA — 202 Accepted (reset enfileirado)", function () {
    expect(res.getStatus()).to.equal(202);
  });
}
