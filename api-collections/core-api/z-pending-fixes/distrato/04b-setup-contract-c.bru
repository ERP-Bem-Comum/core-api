meta {
  name: setup criar contrato C para DIST-5 (data futura)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v2/contracts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{contractsOperatorToken}}
}

body:json {
  {
    "mode": "Active",
    "signedAt": "2026-01-15",
    "sequentialNumber": "102/2026",
    "title": "Contrato E2E Distrato C",
    "objective": "Contrato para teste DIST-5 (terminatedAt futuro)",
    "originalValueCents": 50000,
    "periodStart": "2026-01-01",
    "periodEnd": "2026-12-31",
    "contractor": {
      "type": "supplier",
      "id": "{{contractorSupplierId}}"
    }
  }
}

script:post-response {
  if (res.getStatus() === 201) {
    const id = res.getBody().id;
    bru.setVar("contractIdC", id);
  }
}

tests {
  test("SETUP — contrato C criado (201)", function () {
    expect(res.getStatus()).to.equal(201);
    expect(res.getBody().id).to.be.a("string");
  });
}
