meta {
  name: DIST-6 readerToken sem contract:write retorna 403
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/v2/contracts/{{contractIdA}}/end
  body: json
  auth: bearer
}

auth:bearer {
  token: {{readerToken}}
}

body:json {
  {
    "kind": "Terminate",
    "terminatedAt": "2026-06-01",
    "reason": "Tentativa sem permissao de escrita — deve retornar 403"
  }
}

tests {
  test("DIST-6 — status 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
