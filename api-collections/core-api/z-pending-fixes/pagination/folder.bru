meta {
  name: pagination
  type: folder
  docs: Regressão HTTP-PAGINATION-HARMONIZE — testa shape canônico de meta { currentPage, itemsPerPage, itemCount, totalItems, totalPages }. PAGE-HARM-1/2 reprovam até o fix; PAGE-HARM-3 é guarda (partners já conforme).
}

