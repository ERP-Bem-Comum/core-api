meta {
  name: US3 create email duplicado
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/api/v1/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

body:json {
  {
    "name": "Duplicada",
    "cpf": "52998224725",
    "email": "{{newEmail}}",
    "telephone": "15997133502"
  }
}

tests {
  test("CA — email ja registrado -> 409", function () {
    expect(res.getStatus()).to.equal(409);
  });
}
