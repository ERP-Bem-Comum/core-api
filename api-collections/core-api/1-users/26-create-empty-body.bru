meta {
  name: US3 create body vazio
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/api/v1/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

body:json {
  {}
}

tests {
  test("CA — body sem campos obrigatorios -> 400 (Zod)", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
