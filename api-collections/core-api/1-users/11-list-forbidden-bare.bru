meta {
  name: US1 list sem permissao (bare)
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/v1/users
  auth: none
}

headers {
  Authorization: Bearer {{bareToken}}
}

tests {
  test("SEC — autenticado sem user:list -> 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
