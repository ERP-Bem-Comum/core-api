meta {
  name: US3 create sem permissao (bare)
  type: http
  seq: 21
}

post {
  url: {{baseUrl}}/api/v1/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{bareToken}}
}

body:json {
  {
    "name": "Sem Permissao",
    "cpf": "52998224725",
    "email": "sem-perm-e2e@example.com",
    "telephone": "15997133502"
  }
}

tests {
  test("SEC — sem user:create -> 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
