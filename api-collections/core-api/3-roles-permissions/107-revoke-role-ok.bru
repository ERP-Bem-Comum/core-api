meta {
  name: US4 revoke role do bare
  type: http
  seq: 107
}

delete {
  url: {{baseUrl}}/api/v1/users/{{bareUserId}}/roles/{{adminRoleId}}
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

tests {
  test("CA — 200 revoked:true", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().revoked).to.equal(true);
  });
}
