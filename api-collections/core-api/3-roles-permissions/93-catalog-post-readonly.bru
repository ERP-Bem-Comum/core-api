meta {
  name: US2 catalog read-only (POST 404)
  type: http
  seq: 93
}

post {
  url: {{baseUrl}}/api/v1/permissions
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

body:json {
  {
    "resource": "hack",
    "action": "create"
  }
}

tests {
  test("FR-011 — catalogo imutavel: POST 404", function () {
    expect(res.getStatus()).to.equal(404);
  });
}
