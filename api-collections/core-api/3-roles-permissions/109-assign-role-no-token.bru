meta {
  name: US4 assign role sem token
  type: http
  seq: 109
}

post {
  url: {{baseUrl}}/api/v1/users/{{bareUserId}}/roles
  body: json
  auth: none
}

body:json {
  {
    "roleId": "{{adminRoleId}}"
  }
}

tests {
  test("SEC — 401 sem token", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
