meta {
  name: US7 desativar papel sem token (401)
  type: http
  seq: 218
}

patch {
  url: {{baseUrl}}/api/v1/roles/{{inUseRoleId}}/deactivate
  body: none
  auth: none
}

tests {
  test("US7 — 401 sem Authorization", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
