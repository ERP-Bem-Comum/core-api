meta {
  name: US5 criar papel sem token (401)
  type: http
  seq: 204
}

post {
  url: {{baseUrl}}/api/v1/roles
  body: json
  auth: none
}

body:json {
  {
    "name": "No Token E2E",
    "permissions": []
  }
}

tests {
  test("US5 — 401 sem Authorization", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
