meta {
  name: US3 list roles sem token
  type: http
  seq: 102
}

get {
  url: {{baseUrl}}/api/v1/roles
  auth: none
}

tests {
  test("SEC — 401 sem token", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
