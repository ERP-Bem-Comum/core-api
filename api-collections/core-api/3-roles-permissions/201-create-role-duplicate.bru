meta {
  name: US5 criar papel nome duplicado (409)
  type: http
  seq: 201
}

post {
  url: {{baseUrl}}/api/v1/roles
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

body:json {
  {
    "name": "Gerente E2E",
    "permissions": ["user:read", "user:list"]
  }
}

tests {
  test("US5 — 409 nome duplicado", function () {
    expect(res.getStatus()).to.equal(409);
  });
}
