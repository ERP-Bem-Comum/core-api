meta {
  name: US5 criar papel sem role:create (403)
  type: http
  seq: 205
}

post {
  url: {{baseUrl}}/api/v1/roles
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{bareToken}}
}

body:json {
  {
    "name": "Forbidden E2E",
    "permissions": []
  }
}

tests {
  test("US5 — 403 bare sem role:create", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
