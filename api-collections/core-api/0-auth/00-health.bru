meta {
  name: health check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  auth: none
}

tests {
  test("CA — server no ar (200)", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().status).to.equal("ok");
  });
}
