meta {
  name: collaborators list no auth
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/collaborators
  auth: none
}

tests {
  test("US-005 — GET /collaborators sem token retorna 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
