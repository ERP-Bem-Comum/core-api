meta {
  name: partners aggregate bare user 403
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/partners
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("US-001 — GET /partners sem as 4 reads retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
