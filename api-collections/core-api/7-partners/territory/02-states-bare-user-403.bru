meta {
  name: partner states bare user 403
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/partner-states
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("US-002 — GET /partner-states sem geography:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
