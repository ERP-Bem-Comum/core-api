meta {
  name: export acts csv sem permissao 403
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/acts/export
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("US-002 — GET /acts/export sem act:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
