meta {
  name: BGP create sem auth
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v2/budget-plans
  body: json
  auth: none
}

body:json {
  {
    "year": 2026,
    "programRef": "11111111-1111-4111-8111-111111111111"
  }
}

tests {
  test("BGP — POST /budget-plans sem token retorna 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
