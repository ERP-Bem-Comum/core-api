meta {
  name: BGP create usuario sem budget-plan:write
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v2/budget-plans
  body: json
  auth: bearer
}

auth:bearer {
  token: {{bareToken}}
}

body:json {
  {
    "year": 2026,
    "programRef": "11111111-1111-4111-8111-111111111111"
  }
}

tests {
  test("BGP — POST /budget-plans sem budget-plan:write retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
