meta {
  name: BGP detalhe usuario sem budget-plan:read
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v2/budget-plans/00000000-0000-4000-8000-000000000000
  auth: bearer
}

auth:bearer {
  token: {{bareToken}}
}

tests {
  test("BGP — GET /budget-plans/:id sem budget-plan:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
