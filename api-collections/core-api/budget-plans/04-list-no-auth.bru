meta {
  name: BGP lista sem auth
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v2/budget-plans
  auth: none
}

tests {
  test("BGP — GET /budget-plans sem token retorna 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
