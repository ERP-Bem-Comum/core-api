meta {
  name: BGP options usuario sem budget-plan:read
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v2/budget-plans/options
  auth: bearer
}

auth:bearer {
  token: {{bareToken}}
}

tests {
  test("BGP — GET /budget-plans/options sem budget-plan:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
