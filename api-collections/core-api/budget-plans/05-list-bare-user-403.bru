meta {
  name: BGP lista usuario sem budget-plan:read
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v2/budget-plans
  auth: bearer
}

auth:bearer {
  token: {{bareToken}}
}

tests {
  test("BGP — GET /budget-plans sem budget-plan:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
