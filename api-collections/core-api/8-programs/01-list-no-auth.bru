meta {
  name: programs list no auth
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/programs
  auth: none
}

tests {
  test("PRG — GET /programs sem token retorna 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
