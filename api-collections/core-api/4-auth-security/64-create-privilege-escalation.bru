meta {
  name: SEC escalada por permissao (bare cria)
  type: http
  seq: 64
}

post {
  url: {{baseUrl}}/api/v1/users
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{bareToken}}
}

body:json {
  {
    "name": "Escalada",
    "cpf": "52998224725",
    "email": "escalada-e2e@example.com",
    "telephone": "15997133502"
  }
}

tests {
  test("SEC — usuario sem privilegio nao cria (fail-closed) -> 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
