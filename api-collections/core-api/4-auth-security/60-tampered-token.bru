meta {
  name: SEC token adulterado
  type: http
  seq: 60
}

get {
  url: {{baseUrl}}/api/v1/users
  auth: none
}

headers {
  Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.tampered.payload-invalido
}

tests {
  test("SEC — JWT invalido/forjado -> 401 (verificacao de assinatura)", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
