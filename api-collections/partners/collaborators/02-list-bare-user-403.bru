meta {
  name: collaborators list bare user 403
  type: http
  seq: 31
}

get {
  url: {{baseUrl}}/api/v1/collaborators
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("US-005 — GET /collaborators sem collaborator:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
