meta {
  name: export collaborators csv sem permissao 403
  type: http
  seq: 44
}

get {
  url: {{baseUrl}}/api/v1/collaborators/export
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("US-002 — GET /collaborators/export sem collaborator:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
