meta {
  name: financiers list bare user 403
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/api/v1/financiers
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("CA2 — GET /financiers com usuario sem permissao retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
