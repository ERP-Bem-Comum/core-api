meta {
  name: export financiers csv sem permissao 403
  type: http
  seq: 29
}

get {
  url: {{baseUrl}}/api/v1/financiers/export
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("US-002 — GET /financiers/export sem financier:read retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
