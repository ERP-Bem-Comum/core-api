meta {
  name: suppliers list bare user 403
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/v1/suppliers
  auth: bearer
}

auth:bearer {
  token: {{bareUserToken}}
}

tests {
  test("CA2 — GET /suppliers com usuario sem permissao retorna 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
