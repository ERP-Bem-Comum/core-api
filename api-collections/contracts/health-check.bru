meta {
  name: health check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  auth: none
}

tests {
  test("CA1 — servidor esta no ar (200)", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
