# Sessão 2026-07-31 — limpeza do repo e reconstrução do harness

Registro para retomar o trabalho numa sessão nova. **77 arquivos não commitados** ao fim
desta sessão; ver §"Estado ao encerrar".

**Branch:** `chore/stale-branches-archive`

---

## 1. Limpeza — concluída

|                               | Antes                  | Depois    |
| ----------------------------- | ---------------------- | --------- |
| Disco em `.claude/worktrees/` | 4,7 GB                 | **12 KB** |
| Worktrees                     | 11                     | **0**     |
| Branches locais               | 115                    | **12**    |
| Branches remotas (`origin`)   | 119                    | **17**    |
| PRs abertos                   | 3 travados desde 29/07 | **0**     |
| `handbook/reference/`         | 44 MB                  | **31 MB** |

**Preservações (nada foi perdido):**

- `dc9b56bf` — specs `041-harness-regression-diagnosis` e `042-harness-mechanization`, que existiam
  só na worktree `dev-latest` sem commit. Elas medem a razão `fix:feat`: 10,9% (mai) → 16,2% (jun)
  → **38,8% (jul)**, e mapeiam os 5 enforcements mecânicos que o repo já tinha.
- 6 tags `archive/*` + branch `chore/stale-branches-archive` com `ARCHIVED-BRANCHES.md`.
- Manifesto das 102 branches remotas deletadas (com SHA) em `scratchpad/deleted-branches-manifest.txt`
  — **fora do repo, some quando a sessão morrer**.
- `ERP-Bem-Comum/core-api-deno` — repositório novo, público, com os 45 commits do cutover Deno.
- Deletado: `handbook/reference/mysql/tex-source/*.tex` (13 MB, fonte LaTeX; os 63 chunks `.md`
  gerados dele permanecem).

## 2. PRs mergeados

| PR       | O quê                                                   |
| -------- | ------------------------------------------------------- |
| **#618** | `fix(ci)` — gate `commit-policy` isentando merge commit |
| **#605** | ADR-0055 — Cognito como autoridade de authN             |
| **#608** | guarda de boot do JWT                                   |
| **#610** | enforce da alçada do aprovador                          |
| **#619** | as 12 rules + specs 038/039/040                         |

**Causa do #618:** o gate exigia `Assisted-by:` de todo commit, inclusive merge commits. O ADR-0054 §1
exige o trailer em "commit cujo **conteúdo** foi gerado por IA" — merge integra história, não produz
conteúdo. Qualquer PR atualizado pelo botão "Update branch" do GitHub quebrava sozinho.

## 3. Harness — pipeline W0→W3 em primitivas nativas

| Wave          | Arquivo                                                       | Estado                                                                         |
| ------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| **W0** RED    | `.claude/skills/w0-red/SKILL.md`                              | ✅ classifica **a razão** da falha (tabela de 5 casos); custo zero até invocar |
| **W1** GREEN  | —                                                             | ✅ **absorvido pelo W3**: o Stop hook bloqueia até `pnpm test` passar          |
| **W2** review | `.claude/skills/w2-review/` + `.claude/agents/w2-reviewer.md` | ✅ `context: fork` + `agent:` + `background: false`                            |
| **W3** gate   | `.claude/hooks/stop-quality-gate.sh`                          | ✅ **bloqueante** (`exit 2`), registrado em `settings.json`, timeout 420s      |

**O Stop hook está ATIVO.** A partir do próximo turno com `.ts` alterado, ele roda
`typecheck → format:check → lint → test` e bloqueia se algum falhar. Ele decide sozinho se precisa
rodar (lê `git status`; só `.ts` ou config disparam), porque o gate leva ~145s e o Stop dispara a
cada turno.

**Provado nos dois estados:** bloqueou com `exit 2` num `format:check` vermelho real que ninguém tinha
notado, e liberou com `exit 0` após a correção.

**Memória dos agentes: 15/15.** Os 9 que faltavam ganharam `memory: project`; os 14 ganharam
instrução de escrita no corpo. O `w2-reviewer` registra 4 categorias — falso positivo confirmado,
achado que quase passou, defeito recorrente (candidato a virar rule), regra que mudou.

## 4. `.claude/` e documentação

- **`.zed/` e `.agents/` removidos.** Decisão: **Claude Code é o único agente suportado**.
- **19 skills sem uso desativadas** via `skillOverrides` (7 delas `speckit-*`).
- **Output style customizado removido** — a doc oficial diz que estilos custom **removem as
  instruções de segurança do default**, e o conteúdo dele já estava duplicado no `AGENTS.md`.
- `.claude/.planning` → `context/planning` · `runbooks` → `context/runbooks` · `templates` →
  `context/templates`. O hook `session-start-context.sh` foi ajustado e testado.
- **`context/`** criado com `INDEX.md` — **não é importado por `@`**, de propósito: import carrega
  integral no boot e não economiza nada.
- **`handbook/reference/README.md`** — mapeia as 7 techs com `llms.txt` oficial (testadas por HTTP)
  e as 6 sem.

## 5. Estado ao encerrar — o que decidir na próxima sessão

**`CLAUDE.md.new` está escrito e NÃO aplicado.** 85 linhas (contra 243 do `AGENTS.md`), ~1.339 tokens
contra ~6.585. Aplicar exige:

1. substituir o `CLAUDE.md`, deletar o `AGENTS.md`;
2. **corrigir `tests/cleanup/docs-update.test.ts`**, que lê `AGENTS.md` e roda no gate — que agora
   bloqueia;
3. decidir o que fazer com **3 ADRs aceitos (0037, 0040, 0054) que citam `AGENTS.md`** — são
   imutáveis, e a saída pela hierarquia seria um ADR novo.

**Pendências abertas:**

- `.claude/.pipeline/` — 3.471 arquivos, 45% do repositório. Escopo da spec 038 (US4).
- 4 branches com **50 commits** de trabalho real parado: `feat/devops-foundation` (19),
  `feat/backlog-residual-sdd` (16), `feat/http-contract-detail-contractor` (8),
  `026-email-outbox-observability` (7).
- **Contradição entre as 12 rules nunca verificada** — 574 linhas somadas; um arquivo como
  `src/modules/financial/adapters/http/route.ts` carrega 3 rules ao mesmo tempo. A doc avisa que
  rules contraditórias fazem Claude escolher arbitrariamente.
- **Colisão de ADR-0047**: a branch `docs/adr-0047-cross-module-topology` propõe um `0047` que já
  existe (`0047-transactional-email...`). Quarta colisão de numeração do repo.
- ~~`0034-ocr-port-adapter` diz `Accepted` mas foi superseded pelo `0050`.~~ **Resolvido em
  2026-07-31:** renumerado para `0056` (a colisão com o `0034` do Bruno era o problema maior — o
  `0050` dizia "supersedes ADR-0034" e o índice resolvia isso para o Bruno, que segue vigente) e
  `Status` corrigido para `Superseded by ADR-0050`.
- IPv4 aposentado (`201.23.88.74`) exposto em comentário no `deploy-qa.yml`, repo público.

## 6. Erros que cometi — para não repetir

- **Afirmei "read-only por construção" no W2. Era falso.** `memory: project` habilita `Read`, `Write`
  e `Edit` automaticamente — está documentado. Bloqueei `Edit` via `disallowedTools`; `Write` **não
  pode** ser bloqueado sem quebrar a memória, então restou instrução explícita no corpo do agente.
- **Ia rodar `git worktree remove --force` na `dev-latest`.** Teria destruído duas specs completas e
  os scripts de pipeline. A verificação prévia salvou; a própria spec 038 já registrava esse risco e
  eu não conectei.
- **Quebrei os 3 PRs** ao rodar `gh pr update-branch` — o merge commit gerado não tem `Assisted-by:`.
  O bug era real e pegaria qualquer um, mas o gatilho fui eu.
- **`adapters.md` destilava o read/write split do ADR-0026 como se decisão fosse fato.** A
  autocrítica está certa; a afirmação de que "o código nunca implementou" **não**, e a correção
  aplicada aqui também errou. Verificado em 2026-07-31 contra `722b0371`: a costura `readerUrl`
  **existe** em `contracts` e `partners` (`src/server.ts:201,219`;
  `contracts/adapters/http/composition.ts:235`), e falta em `financial` e `programs`. O texto que
  ficou em `adapters.md:44` — "norma decidida e ainda não implementada", provado por
  `grep -rn "createPool" src/` "não encontra split algum" — é largo demais, e a evidência é falsa: o
  grep devolve 7+ ocorrências (`pool-registry` + 6 drivers de módulo). O que ele não acha é um pool
  _chamado_ reader. Registro verificado alegação por alegação em `context/decisions/ADR-0026.yaml`.
- **Contei branches por linha de tabela em vez de por ADR** (22/13/9 quando era 30/10/4).
- **Vários greps ingênuos** deram falso negativo/positivo: `.tex` casando com "context", `paste`
  quebrado reportando 14 agentes sem uso, `xargs` engolindo o stdin do `git push`. **O shell é
  `fish`** — não faz word splitting; use `bash -c` para loops.
