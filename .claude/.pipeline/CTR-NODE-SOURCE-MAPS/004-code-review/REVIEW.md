# W2 — Code Review (APPROVED)

Round 1.

- Flag `--enable-source-maps` é Stable (`handbook/reference/nodejs/Command-line options.md`).
- Aplicada uniformemente; nenhum script `node ... .ts` ficou de fora.
- Sem impacto em comportamento — apenas melhora a fidelidade de stack traces com strip-types.

APPROVED.
