# W3 — QUALITY — FINANCIERS-HTTP-EDIT

> Skill: `ts-quality-checker` (gate final). **ALL GREEN.**

## Gates (encadeados, exit 0)
```
$ tsc --noEmit            (zero erros)
$ prettier --check .      All matched files use Prettier code style!
$ eslint .                (zero erros/warnings)
ℹ tests 2082 · pass 2065 · fail 0 · skipped 17
```
EXIT_CODE encadeado = 0.

**Ticket pronto para fechar.** PUT /api/v1/financiers/:id no ar (edição cadastral; CNPJ vital exige financier:edit-sensitive).
