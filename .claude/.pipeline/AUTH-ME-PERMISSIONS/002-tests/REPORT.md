# W0 — RED · AUTH-ME-PERMISSIONS
me-permissions.routes.test.ts: /me retorna {userId, permissions}; 401 sem auth; operador com permissões;
user pelado → []. RED: /me hoje retorna só {userId} (sem permissions).
