# W2 — APPROVED
Toggle agora alinhado ao contrato do BFF (SC-005). DTO via response schema Zod (ADR-0027). name
resolvido do catálogo read-only na borda (sem tocar domínio). Sem regressão (15 testes território).
