# W1 — GREEN
partner-geography-plugin.ts: 4 handlers de toggle retornam o DTO (sendResult + response schema Zod);
municipality enriquecido com `name` do catálogo (Municipality.findMunicipalityByCod). Use-cases já
retornavam a view — só o handler descartava. Coleções Bruno (04/08) e contracts/README atualizados.
