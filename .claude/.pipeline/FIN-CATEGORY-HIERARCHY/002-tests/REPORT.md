# W0 — RED (FIN-CATEGORY-HIERARCHY)

Skill: tdd-strategist. Teste em `categories.http.test.ts`: cada item de GET /financial/categories deve expor `parentId`. RED: `item deve ter a chave parentId` (DTO não tinha o campo).
