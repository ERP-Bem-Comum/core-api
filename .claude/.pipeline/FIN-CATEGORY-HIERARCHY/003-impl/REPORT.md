# W1 — GREEN (FIN-CATEGORY-HIERARCHY)

Skill: pipeline-maestro. `parentId` auto-referente em Category/CreateInput/create; coluna `parent_id`+índice (migration 0017); read store drizzle (SELECT+rehydrate); DTO+schema expõem; seed ganha `parentId?` opcional. Documento usa categoryRef (folha). Suíte financial 398/398.
