# W2 — APPROVED (FIN-CATEGORY-HIERARCHY)

Skill: code-reviewer. Aditivo/nullable (back-compat). Sem taxonomia inventada (seeds top-level) — evita Speculative Generality (Fowler, p.68). parentId branded atribuído direto no DTO (lint-clean). Self-ref sem FK física (ADR-0014). APPROVED.
