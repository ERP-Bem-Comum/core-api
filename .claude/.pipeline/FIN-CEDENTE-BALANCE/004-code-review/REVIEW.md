# W2 APPROVED
Skill: code-reviewer. Saldo = closingBalanceCents (buildStatementView, função pura ttestada). N+1 sobre poucas contas (aceitável). Só a LISTA ganha o campo (single-account intacto). Money em string. ADR-0020 (SELECT). APPROVED.
