node_modules/
dist/
coverage/
.tsbuildinfo
cli-state.json
*.log
.DS_Store

# Worktrees git aninhados — cópias completas de src/tests de outras branches
# (sessões paralelas). Têm seu próprio HEAD/formatação; nunca formatar pelo pai.
# Mesma categoria de .claude/worktrees e .agents/worktrees no eslint.config.js.
.claude/worktrees/
.agents/worktrees/
epic/

# Documentação organizacional fica fora — mantemos formatação manual
.claude/
.pipeline/
# Tooling do spec-kit (templates com placeholders, workflows YAML, extensions) — formatação própria,
# mesma categoria do .claude/. Os artefatos de feature gerados ficam em specs/ (esses SÃO formatados).
.specify/
# Handbook (copiado do monorepo pai; preserva formatação original)
handbook/

# Lock files são gerados — não formatar
pnpm-lock.yaml
package-lock.json

# Drizzle Kit artifacts — gerados por `pnpm run db:generate`, não formatar
src/modules/contracts/adapters/persistence/migrations/mysql/meta/
src/modules/auth/adapters/persistence/migrations/mysql/meta/

# Artefatos de relatórios E2E (incluem JSON intencionalmente malformado p/ testes de tamper)
tests/reports/
