# Esta pasta hospeda secrets locais (mysql_root_password.txt, etc.).
# Arquivos .txt dentro são gitignored (ver .gitignore na raiz).
# Use `pnpm secrets:setup` ou `bash scripts/setup-secrets.sh` para criar.
