## ⟳ Nota de estado — 02/09/2026, contra `571a14d7` (1.0.0-rc.2)

Épico vivo, com o layout resolvido e a entrada em produção barrada.

**Layout:** o piloto emite crédito em conta (`01`) e TED (`41`) com câmara, finalidade e P013
medidos no validador — `batch-profile.ts:42-45`, inquiry-0033.

**O que impede a entrada em produção não é o CNAB:**
- **#879** — conta migrada nasce com convênio `LEGADO` e trava para sempre. ⚠️ O fix existe
  (`1837299e`) mas a branch sumiu do remoto em 02/09; o commit está solto, sem referência.
- **#881** — aprovação inoperante em produção: o gate lê `payable:approve` na tabela e o
  `AUTH_RBAC_MODE=bypass` não o alcança.
- **#942** — gerar remessa falha com 5xx opaco em conta-cedente válida com multipag.

Enquanto os três não caírem, o épico não sai do lugar por motivo nenhum de layout.
