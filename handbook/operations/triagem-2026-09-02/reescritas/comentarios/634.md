## ⟳ Nota de estado — 02/09/2026, contra `571a14d7` (1.0.0-rc.2)

O passo 2 do corpo segue pendente: `compose.yaml:369` ainda tem `AUTH_RBAC_MODE: bypass`.

Mudou uma coisa importante em volta dele: o `resolveRbacMode` **voltou a ser fail-secure** no
PR #870 (`rbac-mode.ts:20-21`). Ou seja, o default não é mais o risco — o risco agora é a env
explícita, e só ela. Remover a linha basta; não é preciso mexer no resolver.

Duas issues seguem penduradas nesta:
- **#881** — a aprovação em produção depende de como esse bypass é resolvido.
- **#497** — a segregação de dado sensível em relatórios foi explicitamente empurrada para o
  redesenho de RBAC que esta issue representa.
