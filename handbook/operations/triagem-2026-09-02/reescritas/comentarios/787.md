## ⟳ Escopo restante — medido em 02/09/2026 contra `571a14d7` (1.0.0-rc.2)

O corpo acima diz que nada varre e nada acusa. Isso **deixou de ser inteiramente verdade**.

O descarte de remessa (ADR-0065 §4, PR #850) cobre o caso de `Queued` **sem** arquivo no bucket —
`remittance.ts:170-172`, com a guarda de `:206`.

O buraco que resta é mais estreito e mais difícil: remessa `Queued` **com** arquivo no bucket cujo
`status/` nunca chega. Os workers `van-status-scan` e `van-return-scan` só reagem a objeto que
existe, então essa remessa continua sem varredura, sem alerta e sem filtro — e afunda na ordenação
`generatedAt DESC` conforme novas são geradas.

O resto do diagnóstico do corpo segue de pé: `remittanceListQuerySchema` ainda só tem `page`/`limit`,
e `ListRemittancesInput` ainda é `{ limit, offset }`.
