## ⟳ O código saiu; o rollout não — medido em 02/09/2026 contra `571a14d7` (1.0.0-rc.2)

A consolidação **já está implementada**: `src/workers/runner/run.ts:26` tem `WORKER_GROUP`, e o
`compose.yaml:451-544` roda **4 grupos** em vez dos 6 processos que o corpo acima descreve.

O que falta é aplicar isso no host de QA — registrado em `deploy/qa/README.md:54`.

⚠️ **A aritmética do corpo está desatualizada, e para pior.** Ele cita ~14 pools na borda HTTP,
número do Incident-0001 (10/07). A **#930** mediu **31 pools num boot** — o piso ocioso de 62
conexões já excede sozinho o `max_connections` medido, antes de qualquer worker subir. A economia do
lado dos workers continua valendo, mas o problema principal migrou para a borda.
