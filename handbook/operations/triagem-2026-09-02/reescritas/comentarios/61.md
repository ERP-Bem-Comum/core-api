## ⟳ Metade entregue — medido em 02/09/2026 contra `571a14d7` (1.0.0-rc.2)

**Entregue:**
- `DesfazerConciliacao` — `undo-reconciliation.ts` existe, e o `undo` do repositório
  (`reconciliation-repository.drizzle.ts:358-430`) reverte status e auditoria.
- `DesfazerAprovacao` — `undo-approval.ts`, que não estava no escopo original mas resolve a mesma
  classe de problema.

**Não entregue — é o que sobra desta fatia:**
- `DesfazerPagamento` (`Paid → Approved`). Não existe caminho algum para essa transição fora do
  descarte de remessa, e `remittance-repository.drizzle.ts:469` justifica por escrito **não**
  devolver o título nesse caso.

Vale notar que a #899 achou um efeito colateral do que foi entregue: desfazer a conciliação **não**
desfaz a reclassificação de taxonomia que ela carregou.
