## ⟳ Nota de estado — 02/09/2026, contra `571a14d7` (1.0.0-rc.2)

Já existe **gate de detecção**: `tests/cleanup/schema-collation.test.ts`, que cita esta issue pelo
número.

O que o gate faz é acusar, não consertar. A migration `0050_same_jack_power.sql:5` continua emitindo
`your_number varchar(20) NOT NULL` sem `COLLATE`, ao lado de colunas declaradas com `utf8mb4_bin` —
que é exatamente a divergência que quebra o provisionamento do zero.

O pedido do corpo segue de pé.
