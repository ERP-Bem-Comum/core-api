## ⟳ Nota de estado — 02/09/2026, contra `571a14d7` (1.0.0-rc.2)

A M2 (PR #889) fez a **reclassificação de taxonomia** passar a entrar na trilha — mas projetada como
`DocumentSaved` (`confirm-reconciliation.ts:261`, `timeline/projection.ts:98`), não como evento de
conciliação.

O pedido original continua inteiro: `PayableReconciled` e `ReconciliationUndone` seguem fora da
`fin_document_timeline`. O título ainda chega a "Pago" e a trilha para, mesmo quando ele foi
conciliado.

Relacionado: **#130**, que questiona a projeção da timeline ser síncrona na transação em vez de
projeção (ADR-0022) — mexer aqui provavelmente esbarra nela.
