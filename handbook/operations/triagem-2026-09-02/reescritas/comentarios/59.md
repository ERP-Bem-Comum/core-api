## ⟳ Import entregue, confirmação D+1 não fecha — medido em 02/09/2026 contra `571a14d7` (1.0.0-rc.2)

**Entregue:**
- `ImportarExtrato` com parser multi-formato — OFX, CSV/XLSX e PDF de texto nativo.
- Dedup por FITID.
- Baixa manual com duas origens (ADR-0065 §6, commits `0540daca` / `987a2113`), cobrindo
  `RegistrarPagamentoManual` e o fluxo contingencial.

**Não entregue — é o que sobra desta fatia:**
- A confirmação automática pelo extrato D+1. O `scan-van-returns.ts` **tria e põe em quarentena**,
  mas nenhum caminho do código marca o título como `Pago` a partir do retorno. A transição
  `Transmitido → Pago` só acontece por ação manual.

OCR para extrato digitalizado é escopo à parte e segue na **#145**.
