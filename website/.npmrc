# Docusaurus (webpack + react-loadable) espera node_modules plano para resolver
# os componentes lazy via require.resolveWeak. A árvore simbólica estrita do pnpm
# quebra isso ("require.resolveWeak is not a function" no SSG). hoisted resolve.
node-linker=hoisted
